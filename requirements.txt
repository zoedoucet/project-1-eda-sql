# Minimum versions, not exact pins: most of you are on Anaconda, and hard pins
# fight an existing conda environment.
#
# CHECK BEFORE YOU INSTALL. A current Anaconda install already has all of this,
# and running pip against a conda environment can change versions you did not
# ask it to - we have seen it downgrade numpy, which breaks other packages that
# were compiled against the newer one. So run this first:
#
#   python -c "import pandas, numpy, matplotlib, seaborn, openpyxl; print('all present')"
#
# If that prints, you are done - do not install anything. If it fails, install
# only what is missing, and prefer conda if that is how you set Python up:
#
#   conda install pandas numpy matplotlib seaborn openpyxl
#   pip install -r requirements.txt        # or this, in a fresh venv

pandas>=2.0
numpy>=1.24
matplotlib>=3.7
seaborn>=0.12

# Reading the Online Retail II workbook (option 3 only).
openpyxl>=3.1

# Notebooks. Already present if you installed Anaconda or JupyterLab.
jupyterlab>=4.0
ipykernel>=6.0

# SQLite needs nothing: sqlite3 ships with Python.
# Going the MySQL route instead? Add these two:
# sqlalchemy>=2.0
# pymysql>=1.1
